node_modules/
# Local symlink to skills/ (see CLAUDE.md) — formatting it would rewrite every
# skill twice and confuse git about which path changed.
.claude/

# Generated, not authored — nobody edits these by hand, so holding them to a
# formatting gate only risks a red build on churn. npm writes package-lock.json;
# the dotnet-release-watch workflow writes the release snapshot with jq. Both
# happen to match prettier's output today, which is luck, not a contract.
package-lock.json
.github/state/dotnet-releases.json
